// Cognito login, routing, and the authenticated shell arrive in Phase 1
// (docs/10-roadmap.md, docs/06-frontend.md). This placeholder exists so the toolchain
// (Vite, React, Tailwind, vitest) is exercised end to end during Phase 0 scaffolding.
export function App() {
  return <div className="p-4 text-lg">chat-with-docs-bedrock</div>;
}
